spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/oasis25?sslmode=disable
    driver-class-name: org.postgresql.Driver
    username: ${DB_USERNAME:}
    password: ${DB_PASSWORD:}
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
